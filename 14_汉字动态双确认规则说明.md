# 汉字动态双确认规则说明

## 1. 目标

动态双确认解决两个问题：孩子第一次说对可能刚得到提示；同一天反复说对又不能等同于长期掌握。因此系统同时保存“今日是否独立认出”和跨天 `stage`，两者不能互相替代。

## 2. 什么是独立认出

本轮只显示大汉字，并且孩子没有听朗读、展开拼音/词句/答案、查看联想图或得到家长口头提示，才可点击“我自己认出来了”。

点击任何辅助按钮后，本轮自动标记 `assisted = true`，只能点击“学好了，稍后再自己认”。数据库按没有独立认出处理，稍后生成一张隐藏答案的重试卡。

## 3. 今日通过标准

| 状态 | 标准 |
| --- | --- |
| 新字、stage 0–2 | 连续两次独立认出 |
| stage 3–7 到期复习 | 第一次独立认出即可 |
| 任意阶段本日没有独立认出过 | 改为连续两次独立认出 |

两次确认之间允许穿插其他汉字；只要中间出现“再学一次”或得到帮助，`clean_streak` 清零。今日全部不同汉字通过后，才能领取当天汉字贴纸。

连续三次没有独立认出时，下一张该字自动进入帮助记忆状态，展开拼音、词句和朗读入口；学习后继续回到队尾。之后若再次连续三次未认出，系统会再次提供帮助，但不会因此重复降级。

## 4. 阶段变化

第一次失败采用柔和降级：

| 原阶段 | 新阶段 |
| --- | --- |
| 0 | 0 |
| 1 | 0 |
| 2 | 1 |
| 3 | 1 |
| 4 | 2 |
| 5 | 3 |
| 6 | 4 |
| 7 | 5 |

每个字每天最多降级一次。当天后来连续认出两次，只完成今日任务，不恢复阶段；`due_at` 保持次日优先。没有发生失败时，通过确认后按 stage 1/2/3/4/5/6/7 对应 1/3/7/14/30/60/90 天正常升级，stage 7 再次通过后 180 天维护。

## 5. 队列与进度

- 原始新字/复习字各一张队列项；未通过时追加一张 `same_day_retry` 到队尾。
- `retry_no` 允许同一字当天不限次数重试，但每张队列项只能回答一次。
- 同一时刻一个未通过字只有一张 `pending` 卡，避免重复卡和重复奖励。
- 昨日未完成卡作为今日 `carry` 优先出现，昨日确认次数不跨天继承。
- 页面显示“今日已认出 X / Y 个字”，不显示会动态增加的回答次数。

## 6. 数据表

运行 [supabase/014_dynamic_double_confirmation.sql](./supabase/014_dynamic_double_confirmation.sql) 后：

- `daily_character_progress`：一天一个字一行，保存确认要求、连续确认次数、尝试次数、是否降级和通过时间。
- `daily_session_items.retry_no`：区分同一字的第几张重试卡。
- `learning_attempts` 新增 `assisted`、`attempt_number`、`clean_streak_after`、`daily_passed` 和 `stage_adjusted_today`；所有历史回答仍是不可覆盖记录。
- `answer_queue_item` 在同一个数据库事务里完成鉴权、幂等、阶段变化、历史写入、重试追加和今日进度计算。

普通登录用户对 `daily_character_progress` 只有符合 RLS 的读取权限，不能绕过 RPC 修改今日结果。

## 7. 上线顺序

1. 先备份 `learning_states`、`learning_attempts`、`daily_sessions` 和 `daily_session_items`。
2. 在 Supabase SQL Editor 整段运行 `014_dynamic_double_confirmation.sql`。
3. 再部署包含新版 `lib/actions.ts` 和 `components/learning-experience.tsx` 的前端。
4. 刷新学习页，确认不再出现旧的“还待答 N 次”。

不要只部署前端而不运行 SQL；新版 Server Action 会调用五参数 `answer_queue_item`。

## 8. 验收用例

- [ ] 新字第一次独立认出显示 1/2，并在队尾再出现；第二次独立认出后今日通过并升到 stage 1。
- [ ] stage 3 到期字第一次独立认出即可通过并升到 stage 4。
- [ ] stage 2 第一次没认出降到 stage 1；当天后续再错不继续降级。
- [ ] stage 5 第一次没认出降到 stage 3；当天连续两次认出后仍保持 stage 3、次日到期。
- [ ] 点击汉字朗读、答案或联想图后，本轮不增加 `clean_streak`。
- [ ] 第一次确认后又没认出，确认进度清零。
- [ ] 刷新页面后，尝试次数、确认进度和队列位置仍正确。
- [ ] 全部不同汉字通过后只发一枚贴纸；仍有未通过字时不发。
- [ ] 家长 A 不能读取家长 B 孩子的 `daily_character_progress`。
