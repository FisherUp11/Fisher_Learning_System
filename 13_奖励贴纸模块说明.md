# 小芽贴纸册｜奖励模块说明

## 1. 这个模块解决什么问题

“小芽贴纸册”把线上学习和家里的实体贴纸奖励连接起来。孩子看到的是可爱的贴纸、成长小星星和礼物愿望；家长看到的是有日期、有原因、可核对、可撤销的奖励流水。

奖励只用于鼓励完成与尝试，不显示排名、正确率或惩罚性文案，也不会改变汉字、诗词和音乐原有的学习记录与记忆曲线。

## 2. 当前奖励规则

| 行为 | 获得什么 | 限制 |
| --- | --- | --- |
| 当天全部不同汉字达到动态确认标准 | 1 枚贴纸 | 每个孩子每天最多 1 枚；仍有未通过重试字时不发，刷新不会重复发放 |
| 背诵或朗读一首诗 | 1 颗成长小星星 | 同一首诗同一天最多 1 颗 |
| 歌曲认真跟唱 | 1 颗成长小星星 | 仅播放/听歌不加星；同一首歌同一天最多 1 颗 |
| 乐器辨音练习 | 1 颗成长小星星 | 猜对或没猜对都奖励认真尝试；同一项目同一天最多 1 颗 |
| 节奏练习 | 1 颗成长小星星 | 打出来或暂时不会都奖励认真尝试；同一项目同一天最多 1 颗 |
| 成长小星星累计 3 颗 | 自动兑换 1 枚贴纸 | 每天最多获得 2 颗星，因此额外贴纸至少需要两天 |
| 完成线下数学作业 | 家长手动加 1 枚 | 同一孩子同一天最多 1 次 |
| 特别表扬 | 家长手动加 1–20 枚 | 必须留下独立操作记录 |
| 带入已有线下贴纸 | 家长一次性录入 1–100 枚 | 每个孩子只能初始化一次 |
| 礼物兑换 | 扣除礼物所需贴纸 | 默认建议 10 枚；余额不足不能兑换 |
| 误兑换 | 撤销并返还贴纸 | 原兑换记录保留并标为“已撤销” |

要理问答暂不计入奖励，避免孩子只为了贴纸快速点击；后续确认实际使用节奏后再单独决定。

## 3. 孩子和家长分别在哪里操作

### 孩子：小芽贴纸册

从顶部“学习模块”打开“小芽贴纸册”，或直接访问 `/rewards`：

- 查看当前可用贴纸数；
- 在十格贴纸花园中看到距离下一份礼物还差几枚；
- 查看成长小星星进度；
- 查看家长设置的礼物愿望；
- 查看最近获得、兑换与返还记录。

当天汉字全部完成，或三颗成长小星星集齐时，学习页会弹出简短庆祝卡。庆祝卡可以关闭，也可以直接去贴纸册，不阻碍继续使用。

### 家长：奖励与礼物管理

访问 `/rewards/manage`：

- 给线下数学作业补 1 枚贴纸；
- 录入已有实体贴纸、特别表扬或余额修正；
- 新增礼物并设置需要的贴纸数；
- 上架或下架礼物；
- 确认兑换；
- 撤销误兑换并返还贴纸；
- 多个孩子之间独立切换。

家长手工修正不会删除旧记录。需要减贴纸时必须填写原因，并且余额不能变成负数。

## 4. 数据库设计

运行 [supabase/012_reward_sticker_module.sql](./supabase/012_reward_sticker_module.sql) 后会新增：

| 表 | 用途 |
| --- | --- |
| `reward_accounts` | 每个孩子的十枚目标、三颗星门槛、每日星星上限和当前星星进度 |
| `reward_ledger` | 不可覆盖的贴纸流水；余额始终为全部 `amount` 之和 |
| `reward_growth_events` | 诗词/音乐某项目某天是否获得成长星，以及是否因每日上限未计入 |
| `reward_catalog_items` | 家长维护的礼物愿望和兑换成本 |
| `reward_redemptions` | 礼物兑换、快照和撤销状态 |

数据库函数：

| 函数 | 作用 |
| --- | --- |
| `claim_hanzi_daily_reward` | 验证今天确实有已回答卡且没有未通过字对应的待答卡，再发 1 枚 |
| `register_reward_activity` | 从真实诗词/音乐练习记录反查日期、项目与结果，再登记成长星 |
| `grant_manual_reward` | 数学、特别表扬、初始贴纸和修正 |
| `redeem_reward` | 锁定孩子奖励账户、检查余额、扣除贴纸并保存兑换 |
| `reverse_reward_redemption` | 撤销兑换并追加等额返还流水 |

所有函数都会再次核验当前登录家长是否拥有该孩子。关键操作使用唯一请求编号或业务去重键，因此浏览器重试不会重复记账。

## 5. Supabase 配置

不需要 Supabase Edge Function，也不需要新增环境变量。前端仍部署在 Vercel，奖励判断由 Postgres 函数完成。

1. 先确认 `001`–`011` 已按顺序运行。
2. 打开 Supabase Dashboard → SQL Editor → New query。
3. 打开 [supabase/012_reward_sticker_module.sql](./supabase/012_reward_sticker_module.sql)，复制全部内容并运行。
4. 脚本最后应返回：

```text
reward_table_count = 5
reward_function_count = 5
```

5. 等待十几秒，刷新 App，打开“学习模块 → 小芽贴纸册”。

若启用动态双确认，请继续按顺序运行 `013` 和 [supabase/014_dynamic_double_confirmation.sql](./supabase/014_dynamic_double_confirmation.sql)。贴纸由新版回答 RPC 在 `today_remaining = 0` 时发起领取；新字只完成第一次确认时不会提前发放。

若网页仍显示 schema cache 错误，先确认 `public` schema 已在 Supabase Data API 中暴露，再刷新一次。不要只复制建表部分；RLS、权限和五个函数必须一起运行。

## 6. 首次使用建议

1. 家长先进入“奖励管理”，选择正确的孩子。
2. 若纸质贴纸册已有贴纸，用“带入已有贴纸”录入一次。
3. 新增一份礼物，例如“选择一本新绘本”，成本先设为 10 枚。
4. 孩子完成一次当天汉字学习，确认弹出获得贴纸。
5. 家长完成一次数学手工奖励。
6. 连续两天完成三项不同的诗词或音乐挑战，确认三颗星自动换成 1 枚贴纸。
7. 余额足够后兑换测试礼物，再撤销一次，确认余额恢复而记录仍存在。

第一周建议保留“10 枚换礼物”。若孩子发现目标太远，优先换成更小的礼物，不建议大量提高每日贴纸数量。

## 7. 验收清单

- [ ] 汉字任务还有未通过字或待答重试卡时不发贴纸。
- [ ] 新字只完成第一次独立确认（1/2）时不发贴纸。
- [ ] 完成最后一张汉字卡后只发 1 枚；刷新、重新打开或重复请求不再发。
- [ ] 同一首诗同一天打卡两次，只获得 1 颗成长星，但两次诗词记录都保留。
- [ ] 同一歌曲只听不跟唱，不获得成长星。
- [ ] 辨音和节奏无论结果如何，真实练习一次即可获得成长星。
- [ ] 每天第三个不同的诗词/音乐项目仍保存学习记录，但不再加星。
- [ ] 三颗星自动归零并发 1 枚贴纸；多余星星正确保留。
- [ ] 数学同一天重复提交只加 1 枚。
- [ ] 同一孩子不能第二次导入初始贴纸。
- [ ] 余额不足时不能兑换；兑换后余额正确减少。
- [ ] 撤销兑换后余额恢复，兑换状态显示“已撤销”。
- [ ] 家长 A 看不到也不能修改家长 B 的孩子、贴纸、礼物和兑换。
- [ ] 奖励模块暂时不可用时，汉字/诗词/音乐的原学习记录仍能正常保存。

## 8. 后续迭代边界

建议先真实使用两周，再考虑：

- 家长调整“几颗星换一枚贴纸”或每日星星上限；
- 为礼物加入可选图片；
- 增加每周亲子回顾；
- 把要理问答纳入一种独立、低频的努力奖励；
- 生成可打印的 A4 贴纸册。

无论怎样迭代，都必须保留三个原则：学习历史不可覆盖、贴纸余额由流水求和、奖励失败不能阻断学习记录。
