# 16｜Owner 用户管理、首次改密与重复资源安全清理

## 1. 这次增加了什么

系统现在有三个明确角色：

| 能力 | `owner` 所有者 | `admin` 管理员 | `parent` 家长 |
| --- | --- | --- | --- |
| 查看空间全部孩子概况 | 可以 | 可以 | 仅自己家庭 |
| 审核、发布、归档、分配资源 | 可以 | 可以 | 不可以 |
| 创建登录账号、设置角色/家庭 | 可以 | 不可以 | 不可以 |
| 停用账号、重置临时密码 | 可以 | 不可以 | 不可以 |
| 邀请已有账号加入 | 可以 | 不可以 | 不可以 |
| 合并并永久删除完全重复资源 | 可以 | 不可以 | 不可以 |

结论：**owner 完整继承 admin 的全部权限**，再额外拥有空间级账号和破坏性操作权限。`xiangyufei11@gmail.com` 只要在 `workspace_members.role` 中是 `owner`，登录后就会看到“管理中心 → 用户”。

角色不是存在可由浏览器修改的 `user_metadata`，而是来自 `workspace_members`；页面隐藏只是交互层，真正的权限仍由 RLS 和受限 RPC 再次核验。

## 2. 必须先运行的数据库脚本

已有项目在运行完 `015`、`016` 后，再把下面文件整段复制到 Supabase SQL Editor 执行：

```text
supabase/017_owner_user_management_and_duplicate_cleanup.sql
```

它会：

- 新增 `workspace_user_profiles`，保存账号称呼和“首次登录必须改密”标记；
- 新增 `private.is_workspace_owner`；
- 将成员目录、邀请、永久删除收紧为 owner 专属；
- 新增创建/修改空间账号、完成首次改密和重复资源合并 RPC；
- 回填已有成员资料，不修改已有 Auth 密码；
- 给旧汉字册、诗词册、音乐和问答册补内容指纹；
- 不删除任何现有用户、孩子、学习记录或 R2 文件。

运行后验证：

```sql
select
  auth_user.email,
  member.role,
  member.status,
  profile.display_name,
  profile.must_change_password
from public.workspace_members member
join auth.users auth_user on auth_user.id = member.user_id
left join public.workspace_user_profiles profile on profile.user_id = member.user_id
order by member.joined_at;
```

你自己的行应为 `role = owner`。如果不是，不要在前端硬编码邮箱；先在 SQL Editor 查清正确的空间和账号，再修正该成员角色及 `learning_workspaces.owner_user_id`。

## 3. Supabase 服务端密钥怎么填

账号创建和密码重置要调用 Supabase Auth Admin API，因此 Next.js 服务端需要一把只在服务器使用的密钥。

### 推荐：新的 Secret key

1. 打开 Supabase Dashboard。
2. 进入对应 Project → **Project Settings → API Keys**。
3. 在 Secret keys 区域创建/复制一把以 `sb_secret_` 开头的 key。
4. 本机 `.env.local` 填：

```dotenv
SUPABASE_SECRET_KEY=sb_secret_这里粘贴完整密钥
```

5. Vercel → 对应项目 → **Settings → Environment Variables**，新增同名变量，勾选 Production；需要预览环境测试时再同时勾选 Preview。
6. 保存后必须 Redeploy，旧 Deployment 不会自动得到新变量。

### 旧项目兼容：Legacy service_role key

如果 Dashboard 暂时没有 Secret key，可在 **Project Settings → API Keys → Legacy API keys** 找到 `service_role` 对应的完整 JWT，填：

```dotenv
SUPABASE_SERVICE_ROLE_KEY=eyJ...这里粘贴完整的 service_role JWT
```

两者只需一个；代码优先读取 `SUPABASE_SECRET_KEY`，没有时才读取旧的 `SUPABASE_SERVICE_ROLE_KEY`。不要把 Project URL、anon key、publishable key 或数据库密码填到这里，它们不是 Auth Admin 密钥。

### 安全红线

- 变量名绝不能带 `NEXT_PUBLIC_`。
- 不要把真实值写进 `.env.example`、Git、截图、CSV、聊天或浏览器代码。
- 这把密钥绕过 RLS；只能由 `lib/supabase/admin.ts` 的服务器代码读取。
- 页面执行账号操作前，仍会先用普通登录会话确认当前用户确实是 owner；持有客户端 anon key 的普通用户不能调用 Auth Admin API。
- 怀疑泄漏时，在 Supabase 立刻撤销/轮换 Secret key，并同步更新 Vercel。

## 4. 创建新账号的完整流程

入口：**学习模块 → 管理中心 → 用户**。

1. 填登录邮箱和账号称呼。
2. 选择角色：
   - `家长`：必须加入已有家庭，或同时新建家庭；
   - `管理员`：能管理全空间内容和孩子，但看不到 owner 专属用户页。
3. 临时密码可留空，由服务器生成；也可手工设置至少 12 位且包含字母、数字的临时密码。
4. 创建成功后，页面只显示一次“邮箱 + 临时密码”，立即复制并用安全方式交给对方。
5. 对方首次登录会被强制跳到 `/account/change-password`；设置自己的新密码后才能进入学习模块。

系统用 Auth Admin API 创建登录身份，再在同一次业务流程中建立 `workspace_members`、`family_members` 和 `workspace_user_profiles`。如果数据库归属建立失败，服务器会尽力回滚刚创建的 Auth 用户，避免“能登录但没有家庭”的半成品账号。

如果邮箱已经在 Supabase 注册，系统不会强行覆盖其密码。请改用 **管理中心 → 邀请** 生成一次性链接，让现有账号自行登录并接受；接受后会自动补齐用户目录资料。

## 5. 修改、停用与重置密码

owner 可以在账号卡片中修改称呼、`admin/parent` 角色、所属家庭和状态。

- **停用**只把成员状态设为 `suspended`；孩子、资源分配和所有历史保留，对方无法再进入空间。
- **恢复**把状态改回 `active` 并恢复选择的家庭关系。
- **重置临时密码**会生成一次性新密码，并把 `must_change_password` 设为 `true`；对方下次登录先改密。
- owner 不能在此页降低、停用或重置自己的账号，防止把唯一最高权限锁在系统外。owner 自己改密请走正常的账户安全流程。
- 当前产品不提供“删除 Auth 用户”按钮。家庭离开时优先停用，这样审计、`recorded_by` 和历史解释仍完整。

## 6. 重复资源如何安全清理

入口：**管理中心 → 资源**。相同类型、相同 SHA-256 内容指纹的资源会显示“可能重复”。owner 可在重复项下展开“清理这份重复内容”，选择一份已审核发布的资源作为保留项。

数据库在一个事务内执行：

1. 再次确认当前用户是 owner；
2. 确认保留项和删除项属于同一空间、同一类型、完整文字/顺序/备注指纹一致；音乐还会比较媒体类型、原文件名、MIME、大小、标签和顺序；
3. 把所有孩子分配合并到保留项，活跃关系优先保留；
4. 汉字的 `active_package_id` 如指向删除项，会改为保留项；
5. 写入 `workspace_audit_events`；
6. 最后删除重复根资源。

不同模块的数据保护策略：

| 模块 | 已有历史时是否允许安全合并删除 | 原因 |
| --- | --- | --- |
| 汉字册 | 允许 | 学习状态/尝试引用公共 `character_id`，删重复字册只删字册关联 |
| 诗词册 | 允许 | 背诵历史引用公共 `poem_id`，删重复册不删诗词历史 |
| 音乐 | 不允许 | 状态/尝试直接引用该 `music_item`；有历史时必须归档 |
| 要理问答册 | 不允许 | 每次导入产生版本内问题 ID；有历史时必须归档 |

音乐无历史、文字资料相同且媒体清单一致时才允许合并；成功后服务器会继续删除被移除内容对应的 R2 对象。如果 R2 未配置或个别对象删除失败，数据库合并不会回滚；页面会明确提示需要到 R2 手工检查的旧对象数量。数据库没有保存 MP3 的逐字节哈希，因此即便文件名和大小相同，owner 仍应先试听确认。

“指纹相同”只是自动清理的必要条件。内容不完全相同、译本不同、音频版本不同或不确定时，使用**归档**，不要为了整洁永久删除。

## 7. 数据与安全边界

新增/关键对象：

- `workspace_user_profiles`：账号显示名、首次改密标记；不保存明文密码。
- `owner_provision_workspace_user`：owner 原子建立空间/家庭归属。
- `owner_update_workspace_user`：owner 修改非 owner 成员角色、状态和家庭。
- `complete_initial_password_change`：只清除当前登录用户自己的首次改密标记。
- `owner_merge_duplicate_resource`：只允许 owner 合并指纹相同资源，并保护音乐/问答历史。
- `workspace_audit_events`：记录创建账号、权限修改、密码重置和重复资源合并；不记录任何密码。

临时密码只在 Auth 中以 Supabase 的安全方式处理，不写应用数据库、审计表或日志。Server Action 返回给创建者一次后，页面刷新即消失。

## 8. 验收清单

- [ ] 运行 `017` 后，自己的账号显示为 owner，旧孩子和历史数量未减少。
- [ ] 没配置 Secret key 时，“用户”页显示明确配置提示，不泄露服务器变量。
- [ ] owner 创建家长账号，成功页出现一次临时密码。
- [ ] 新账号登录后不能直接进 `/learn`，必须先设置 12 位新密码。
- [ ] 家长只看自己家庭；admin 可看全空间看板、审核和分配，但看不到“用户/邀请”。
- [ ] 停用家长后，对方无法进入空间；恢复后孩子和历史仍在。
- [ ] 重置密码后，旧密码失效，新临时密码可登录且再次强制改密。
- [ ] 两份相同测试字册可合并，孩子分配仍指向保留字册，学习次数不变。
- [ ] 有音乐练习历史的重复项无法永久删除，错误提示要求归档。
- [ ] `workspace_audit_events` 能看到 `user.provisioned`、`user.updated`、`user.password_reset` 或 `resource.duplicate_merged`。

## 9. 后续 AI Agent 约束

1. `owner` 必须始终包含 `admin` 能力，不要另写一套互斥判断。
2. Auth Admin client 只能出现在 `server-only` 文件或 Server Action；绝不能传入 Client Component。
3. 新增 owner 能力时同时做页面校验、Server Action 校验和数据库 RLS/RPC 校验。
4. 不把密码写入任何 `public` 表；不在审计详情保存密钥或临时密码。
5. 用户离开默认停用，不直接删除 Auth 用户或孩子学习历史。
6. 资源永久删除必须先证明历史引用安全；不确定时只归档。
7. SQL 继续新增编号迁移，不回改已在线执行的 `001`–`017`。
