# 汉字重点字｜家长使用、调度规则与验收

这个功能用于把线下绘本中的 60、80 个常用字与 APP 学习配合起来。家长可以从孩子所有已导入字册中勾选重点字；系统让这些字优先进入学习，但仍使用原有的记忆曲线、答错降级和历史记录。

## 1. 第一次启用

在部署包含本功能的前端前，先打开 Supabase Dashboard → SQL Editor，完整运行：

[supabase/011_priority_character_learning.sql](./supabase/011_priority_character_learning.sql)

脚本会：

- 新建 `learner_character_priorities`；
- 为新表启用 RLS，只允许家长管理自己的孩子；
- 新增原子保存 RPC `set_character_priorities`；
- 升级 `get_library_rows`，加入重点字统计、筛选和分页；
- 升级 `get_today_queue`，让到期重点字和未学重点字优先；
- 保留 `answer_queue_item` 原样，不改变任何答题阶段或历史。

脚本不会删除、重置、自动降级已有学习记录，也不需要增加环境变量或 Supabase Edge Function。

## 2. 家长怎样选择

1. 打开底部“册”。
2. 先选择孩子；需要时按“来源字册”、学习状态或搜索文字缩小范围。
3. 勾选每个字左侧的“优先学”，也可以点“本页全选”。
4. 点击“保存本页重点字”。
5. 页面显示保存成功和这个孩子当前重点字总数。可继续翻页选择。
6. 以后取消勾选并再次保存，即可取消重点；学习阶段和历史不会被删除。

“重点字”筛选支持：

- 全部重点字；
- 重点但尚未开始；
- 重点且正在巩固；
- 重点且稳定掌握。

若同一个字出现在多次 CSV 导入中，它仍然只有一个孩子级重点标记和一份学习状态。

## 3. 推荐队列规则

每天第一次生成汉字任务时按以下顺序：

1. 以前没有答完的卡片全部带入；
2. 已经到期的重点字；
3. 已经到期的普通字；
4. 尚未学习的重点字（可来自孩子任意已关联、已发布字册）；
5. 当前激活字册中的普通新字，继续按原 CSV 顺序；
6. 当天首次学习或答错后追加的强化卡。

重要边界：

- 重点字不会在 `due_at` 之前提前复习；
- 重点字占用原有的每日新字名额，不会额外增加学习量；
- 到期复习和旧日未完成卡片的原上限/补带规则不变；
- 当天已经生成的固定队列不在中途重排，新设置从后续尚未生成的队列生效；
- 重点标记不自动取消。阶段 5 以上会显示稳定掌握，仍按长期间隔维护。

## 4. 漏学一两天时

漏学不会删除任务，也不会仅因“没有打开 APP”而自动降级：

- 上次未答完的卡片回到下一次任务前面；
- `due_at <= 当前时间` 的字保持到期状态；
- 重点字在到期候选中排在普通字前；
- 阶段只在孩子回答“我认识 / 再学一次”后变化。

这仍是原来的固定间隔复习：stage 1–7 对应 1、3、7、14、30、60、90 天；stage 7 再答对后 180 天维护。

## 5. 数据与权限

`learner_character_priorities` 的主键是：

```text
(learner_id, character_id)
```

这样同一个字可对哈森是重点、对另一个孩子不是重点。重点没有放进 `characters`，因为内容表不应保存孩子个人安排；也没有放进 `learning_states`，因为尚未学习的字还没有状态。

批量保存通过 `set_character_priorities` 在一次事务内完成。RPC 会重新验证：

- 当前登录用户拥有这个孩子；
- 单次管理不超过 100 个字；
- 所选字确实属于该孩子已关联、已发布的字册；
- 不能通过浏览器参数给其他孩子或外部字库设置重点。

## 6. 上线验收

建议先用 5–10 个字完成：

- [ ] 从第一份和第二份 CSV 各选择重点字，保存后都能显示“重点”。
- [ ] 切换到“重点 · 尚未开始”，数量和列表正确。
- [ ] 每天新字设为 5，今日新字不超过 5，且先出现未学重点字。
- [ ] 一个未到期重点字不会因为设为重点立即出现。
- [ ] 把重点字的 `due_at` 调到过去后，它排在普通到期字之前。
- [ ] 重点字点“再学一次”仍按原规则降两级并追加当天强化。
- [ ] 取消重点后，回答次数、阶段、到期日和历史均保留。
- [ ] A 家长不能读取或修改 B 家长孩子的重点字。

可在 SQL Editor 做结构检查：

```sql
select table_name
from information_schema.tables
where table_schema = 'public'
  and table_name = 'learner_character_priorities';

select routine_name
from information_schema.routines
where routine_schema = 'public'
  and routine_name in ('set_character_priorities', 'get_today_queue', 'get_library_rows')
order by routine_name;
```
