export default function AppLoading() {
  return <section className="panel page-loading" aria-live="polite" aria-label="正在切换页面">
    <span className="loading-leaf">芽</span>
    <div><strong>正在打开…</strong><p>字芽正在准备这一页。</p></div>
  </section>;
}
