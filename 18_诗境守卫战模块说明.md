# 18｜诗境守卫战使用、配置与架构说明

## 1. 这次实现了什么

“诗境守卫战”是原诗词背诵模块的游戏入口，不替代原来的诗词册、正文页、手工打卡和评分。

- 入口：顶部“学习模块 → 诗词背诵”，再点底部“诗境”；也可从单首诗正文点“驾驶小坦克复习这首诗”。
- 家长可先选择孩子，再从该孩子所有已分配诗词中选择一首生成游戏。
- 电脑默认进入 7 分钟完整玩法：热身、点亮诗堡、追击下句、诗句排序、诗卷 Boss。
- 手机默认进入轻量玩法：先看/听全文，再点中正确诗句小坦克，最后藏起正文完整背诵。
- 每次正确命中都会慢读正确诗句；Boss 完成后慢读整首诗。Azure Speech 不可用时退回设备中文朗读。
- 游戏结束会先自动保存场次和逐题事实；家长再听孩子真实背诵，选择 1–10 分。没有评分时，游戏证据仍然保留。
- 家长评分会新增一条原有 `poem_recitation_attempts` 记录，因此诗词册原来的次数、最近评分、贴纸成长星逻辑仍然生效。

## 2. 必须先运行的 Supabase SQL

打开 Supabase Dashboard → SQL Editor → New query，将下面文件的**全部内容**复制进去并运行：

```text
supabase/018_poem_tank_game.sql
```

它依赖已经运行完成的 `008`、`012`、`015`，应当在 `001–017` 之后运行。脚本可以重复运行，不会删除旧诗词、背诵、汉字、音乐、问答或奖励记录。

运行后检查：

```sql
select to_regclass('public.poem_game_sessions') as sessions,
       to_regclass('public.poem_game_attempts') as attempts,
       to_regclass('public.learner_poem_line_states') as line_states,
       to_regclass('public.poem_game_maps') as maps;

select routine_name
from information_schema.routines
where routine_schema = 'public'
  and routine_name in ('record_poem_game_result', 'rate_poem_game_session');
```

第一段四列都应显示表名，第二段应返回两个函数。

## 3. AI 地图与稳定降级

游戏不会在每一局都等待 GPT。页面先立即按诗中的“月、江河、田野、山林、花木、雪”等语义生成一张稳定地图；后台再尝试使用现有 Azure OpenAI 文本部署生成更贴合诗意的场景蓝图，并缓存到 `poem_game_maps`。

使用现有变量即可，不需要新增密钥：

```dotenv
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=你的-gpt-4.1-部署名
AZURE_OPENAI_API_VERSION=2024-10-21
```

- 若 `AZURE_OPENAI_DEPLOYMENT` 指向 Azure 中的 GPT-4.1 部署，地图会由该部署生成。
- 未配置、超时、返回格式不合格或模型临时不可用时，页面继续使用稳定地图，游戏和保存都不受影响。
- AI 只返回地图名称、简介、标签、柔和色板、地标和天气；不生成题目答案，不决定孩子分数，也不接触其他家庭的数据。
- 当前不需要 Supabase Edge Function，也不需要 Phaser。Canvas 游戏在 Next.js Client Component 中运行；权限、AI 密钥和保存动作仍在服务端。

## 4. 数据为什么要分四层

| 表 | 保存什么 | 不保存什么 |
| --- | --- | --- |
| `poem_game_maps` | 每首诗经校验的 AI/稳定地图蓝图缓存 | 不保存孩子成绩 |
| `poem_game_sessions` | 一局的时长、模式、完成阶段、对错汇总、可空家长评分 | 不覆盖原背诵历史 |
| `poem_game_attempts` | 每一次击中选择、首次是否答对、反应时间、对应诗句 | 不合并同一局的重复尝试 |
| `learner_poem_line_states` | 孩子对每句诗的累计暴露、对错、0–100 熟练度、下次建议复习时间 | 不代替整首诗家长评分 |

浏览器只提交“发生了什么”。`record_poem_game_result` 在一个数据库事务中核验孩子归属、诗词分配、写入场次与逐题记录，并计算逐句熟练度；普通客户端不能直接改熟练度。答错的句子建议次日再见，稳定答对后间隔逐步从 2、4、7 天延长到 14 天。这个逐句状态目前只为后续推荐准备，不会改变原诗词列表的温和推荐，也不会调用汉字复习函数。

## 5. 家长评分规则

游戏中的命中率是“看到提示后的识别表现”，不是自动背诵评分。结算页必须由家长听孩子背一次后决定：

- 9–10：很熟，可以完整、流畅背出；
- 7–8：基本会背，有少量停顿；
- 5–6：能背一部分，还需练习；
- 1–4：目前需要多听、多读、多背；
- 暂不评分：不点击评分即可，游戏场次仍保存。

一局只能生成一次背诵评分。网络重试使用 `client_session_id` 和数据库唯一约束去重，不会重复写游戏或评分。

## 6. 上线前验收

1. 在电脑打开 `/poems/game`，切换孩子、诗词后提交，标题与正文应同步变化。
2. 开始完整玩法，用方向键/WASD 移动、空格发射；连接手柄时 A 键可发射。
3. 正确命中后应慢读诗句，当前阶段完成后约 0.65 秒进入下一阶段，不应长时间等待。
4. 坦克活动区限制在画布安全边界内，不应卡到最上方不可射击区域。
5. 完成或提前结束后，`poem_game_sessions` 新增一行，`poem_game_attempts` 保存每次答案。
6. 家长选择分数并保存后，`poem_recitation_attempts` 新增一行，原诗词详情页的次数与最近评分更新。
7. 用浏览器手机尺寸打开，同一路由应自动显示轻量模式；正文、三个诗句坦克和最终背诵按钮均可单手操作。
8. 临时移除 Azure OpenAI 配置后，仍能看到“稳定诗意地图已准备”并完成游戏。

## 7. 当前架构评估

这次采用“独立游戏事实 + 复用原诗词评分”的结构，原有模块不需要重写，架构是可继续迭代的：

- 游戏帧循环不写数据库，只有结算时一次原子保存，避免频繁请求和半局脏数据。
- AI 蓝图可缓存、可降级，不在孩子学习主路径上形成单点故障。
- 手机和电脑共享题目/记录协议，但分别呈现适合设备的交互。
- 家长评分仍是整首诗的权威结论；逐句状态只提供以后“今天该复习哪句”的依据。
- RLS 只允许当前家庭或管理员读取孩子记录，写入仅经两个受限函数完成；新表显式授予 `authenticated` 读取/函数执行权限，兼容 Supabase 新项目更严格的 Data API 默认授权。

后续若继续做“按遗忘曲线自动推荐今日游戏”，应读取 `learner_poem_line_states.next_due_at` 生成候选，不要把诗句塞入汉字的 `learning_states`。

