# 操作反馈、音乐歌单与导入防重

更新日期：2026-09-10。此次只优化操作体验，不改变学习规则、复习间隔、打卡次数或贴纸计算，也不删除既有数据。

## 1. 音乐怎样多选循环播放

1. 打开「学习模块 → 音乐天地」。多孩子家庭先选好孩子。
2. 在「全部」或「唱一唱」分类，找到「选一份今天的歌单」。
3. 勾选歌曲。可以搜索名称或分类，也可以全选当前搜索结果。
4. 点击「开始循环播放」。播放顺序就是勾选顺序，最后一首播完后自动回到第一首；只选一首时单曲循环。
5. 播放区可暂停、继续、上一首、下一首、结束听歌，也能展开歌词和歌单。
6. 播放过程中可重新勾选，但只有按「播放所选，更新歌单」才会替换当前队列。

只列出当前孩子已分配且有音频的歌曲；服务器在每次播放前再次检查有效分配、已发布、已审核以及登录权限。没有封面不影响使用。曲目失效或加载失败时会提示重试/跳过，不会自动当作练习成功。

边界：歌单仅保存在当前页面，刷新、离开页面或切换孩子后停止，需要重新选择；本版不新增“保存歌单”数据库。手机锁屏或后台播放取决于系统和浏览器，不能保证持续播放。单纯听歌不自动增加学习次数或发贴纸，唱过后仍在歌曲详情里打卡。

## 2. 导入与保存反馈

汉字 CSV、诗词 CSV、要理问答 CSV 都采用同一流程：

1. 填写内容、选择孩子和文件。
2. 点击导入，先弹出确认框，显示名称、文件和孩子。要理问答还会显示发布选项。
3. 确认后锁定表单，显示正在处理；超过 8 秒继续提示等待，不自动再次提交。
4. 完成后弹窗告知导入结果，页面保留一条结果提示。
5. 失败时保留已填写信息和所选文件，可修正或重试；成功后清除所选文件，避免误点再次上传。

孩子设置、音乐资料保存、音乐内容创建和 MP3/图片上传也有明确反馈。音乐资料保存显示数据库确认后的发布状态，并保留用户填写的字段；不再为保存结果额外刷新整页。创建孩子成功后清空创建表单，避免再次创建同名档案。

普通学习打卡没有增加每次确认弹窗，孩子学习时的操作保持简洁。

## 3. 重复导入与中断怎么办

系统对三类 CSV 的实际解析内容计算指纹，而不是只比较文件名或册子名称。

- 同一账号提交相同内容，包括同时连点或网络中断后重试：复用同一册，不再重复创建。
- 相同内容选择另一位孩子：按当前账号权限复用已有资源并确认分配，不复制一份内容。普通家长仍需管理员审核/分配。
- 上次只有部分内容写入：新版创建的本人导入草稿可以用原文件续传，内容齐全后才发布/送审。
- 内容写入成功但分配失败：再次导入原文件会尝试完成分配，不重建资源。
- 已归档资源：不会通过重复导入自动恢复，按提示去资源管理处理。
- 旧版本留下的不完整目录：不会擅自删除或覆盖，提示管理员检查。

相同内容但修改了显示顺序，或真实修改了正文，可能视为不同版本。导入不会覆盖空间内已有同一汉字或同一 `poem_key` 的正文；修订已有内容请使用原来的资源编辑界面。

本轮严格保障的是**同账号**的并发去重，使用已有 `UNIQUE(created_by, code)` 约束。不同账号同时首次上传相同内容仍可能各建一册；已有且当前账号可见的重复资源会优先复用，不越过家庭权限查阅私有待审素材。历史重复资源不自动清理，owner 可继续使用原来的安全合并/归档工具。

MP3/图片上传也先确认并锁定表单。同一页面里，如果文件已到 R2 但登记结果丢失，重试会复用已上传对象，服务端按对象路径识别已完成登记。刷新页面后不会保留这个临时上传状态。

## 4. 按钮响应优化范围

- 底部导航和学习模块菜单点击后立即显示正在打开的目标。
- 删除启动时批量预取所有模块的请求，改为悬停、聚焦、触摸时按需预取，避免请求争抢网络。
- 服务端 Supabase 客户端与权限上下文在同一请求内复用，不跨账号缓存私有数据。
- 关键保存/导入有同步防连点锁；JavaScript 尚未加载好时不允许提前提交。

这些改动减少不必要的请求并改善等待反馈，但不能保证远端网络或 Supabase 延迟消失。如持续超过十几秒，应继续查看对应 Vercel 请求日志，不要连续点击。

## 5. 如何上线

现有系统的数据库迁移和 R2 已配置完成时，本轮**无需运行新的 SQL、无需增加环境变量**。

1. 本地停止旧开发进程，再执行 `npm run dev` 测试。
2. 核对本次改动后提交到自己的 GitHub 仓库，让 Vercel 部署最新代码；本次开发未替你推送或部署。
3. 在 Vercel 等待部署显示 Ready，再刷新正式域名页面。
4. 测试两首真实歌曲播放完后能回到第一首；也试试暂停和切换孩子。
5. 导入同一 CSV 两次，第二次应提示已有资源，目录数量不增加。
6. 保存音乐资料为“已发布”，确认结果弹窗和刷新后的状态一致。

## 6. 开发者交接与测试

关键实现：`components/feedback-form.tsx`、`components/music-playlist.tsx`、`app/api/music/playlist-audio/route.ts`、`lib/import-safety.ts`。其它模块继续走既有数据结构和权限模型。

本地导入回归可运行：

```bash
node --test scripts/test-resource-imports.cjs
npm run lint
npx tsc --noEmit
npm run build
```

导入测试使用实际 Server Action 代码与内存 Data API 模拟，覆盖三类资源的同账号并发、不同孩子复用、中断恢复、发布/分配失败重试、归档保护和权限检查；不会连接正式数据库。生产环境的 PostgreSQL RLS 和真实 R2 音频仍应按上面的上线步骤验收。

本轮验证结果：

- 27 项导入回归全部通过。
- Chrome 本地模拟页面：确认取消与焦点恢复、双击只提交一次、处理中锁定、失败后重试、完整字段/文件传递、成功清除文件和重复结果弹窗通过。
- 使用实际短音频的浏览器播放测试：播完切到下一首、最后一首回到第一首、暂停/继续、上一首/下一首、失效歌曲跳过、卸载后停止通过。测试音频由本地模拟响应提供，未读取家庭的真实 R2 文件。
- 390 像素手机宽度无横向溢出；导航即时提示、未登录跳转、无 JavaScript 提交保护通过，最终回归未出现页面或 hydration 错误。
- ESLint、TypeScript 与生产构建通过；临时测试页面已删除，不会部署。

没有在正式 Supabase 写入测试资源，也没有替用户推送 GitHub 或部署 Vercel。
